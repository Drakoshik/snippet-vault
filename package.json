{
  "scripts": {
    "prod:backend": "cd backend && npm run build && npm run start",
    "prod:frontend": "cd frontend && npm run build && npm run start",
    "prod": "npm-run-all --parallel prod:backend prod:frontend",
    "dev:backend": "cd backend && npm run start:dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "setup:backend": "cd backend && npm ci && npm run first:setup",
    "setup:frontend": "cd frontend && npm ci",
    "first:setup": "npm ci && npm-run-all --parallel setup:backend setup:frontend"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
